2. 为什么请求接口会在后面接?\_t=

在请求接口后面接 ?\_t= 通常是为了添加一个时间戳参数。

添加时间戳参数可能有以下几个原因：

防止缓存：浏览器或服务器可能会对相同的请求进行缓存，以提高性能。通过添加不断变化的时间戳，确保每次请求都被视为新的请求，从而获取最新的数据。
跟踪和分析：对于后端服务器来说，时间戳可以用于跟踪和分析请求的时间分布，了解用户的访问模式和系统的负载情况。
防止重复提交：在某些情况下，时间戳可以用于验证请求的唯一性，防止重复提交相同的请求。

总的来说，?\_t= 后面的时间戳参数有助于提高系统的性能、安全性和数据的准确性。
