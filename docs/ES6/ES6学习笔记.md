# ES6学习笔记

## let 和 const 命令

**1. let 命令**

> **块级作用域**

> **不存在变量提升**

> **暂时性死区**

> **不允许重复声明**

**2. 块级作用域**

> **块级作用域与函数声明**

**3. const 命令**

> **基础用法**

> **ES6 声明变量的6种方法**

**4. 顶层对象的属性**

**5. global 对象**

## 变量的解构赋值

**1. 数组的解构赋值**

> **基础用法**

1. set 2. map

> **默认值**

**2. 对象的解构赋值**

**7. 用途**

> (1) 交换变量的值

> (2) 从函数返回多个值

> (3) 函数参数的定义

> (4) 提取JSON数据（重点）

## 字符串扩展

> **1. codePointAt()**

> **2. String.fromCodePoint()**

> **3. 字符串遍历接口**

> **4. normalize()**

> **5. includes(), startsWith(), endsWith()**

> **6. repeat()**

> **7. padStart(), padEnd()**

> **8. matchAll()**

> **9. 模板字符串**

## 正则的扩展

> **1. 字符串的正则方法**

> **2. u修饰符**

> **3. i修饰符**

> **4. y修饰符**

> **5. g修饰符**

> **6. s修饰符**

> **7. RegExp.prototype.unicode 属性**

> **8. RegExp.prototype.sticky 属性**

> **9. RegExp.prototype.flags 属性**

> **10. 具名组匹配**

> **11. 解构赋值和替换**

## 数值的扩展

> **1. 二进制和八进制表示法**

> **2. Number.isFinite(), Number.isNaN()**

> **3. Number.parseInt(), Number.parseFloat()**

> **4. Number.isInteger()**

> **5. Number.EPSILON**

> **6. 安全整数和 Number.isSafeInteger()**

> **7. Math.trunc()**

> **8. Math.sign()**

> **9. Math.cbrt()**

> **10. Math.imul()**

> **11. Math.hypot()**

> **12. Math.expm1()**

> **13. Math.log1p()**

> **14. Math.log10()**

> **15. Math.log2()**

> **16. 指数运算符**